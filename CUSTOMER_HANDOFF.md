# Customer Handoff v2.0

## 产品是什么

Video Remaker 是一个本地网页端短视频复刻自动化工作台。它面向短视频内容团队，用于把“参考视频的结构、节奏和表达方式”转化为原创改编方案，并生成可预览、可下载的本地成片资产。

它的目标是：

```text
同款结构，原创内容
```

它不是搬运工具，不自动下载公开视频，也不绕过平台限制。

## 复刻的 Bloome Agent 群组逻辑

v2.0 已把 Bloome 原型中的多 Agent 协作落成本地产品流程：

- Storyboard Agent：拆解结构、分镜、节奏和画面语言。
- Content Creator Agent：生成原创改编脚本、素材清单、视频 prompt 和发布文案。
- Review Agent：检查原创性、可执行性、prompt 质量和生成风险。
- Orchestrator：串联输入、生成、审稿、资产生成、合成和导出。

## 当前稳定工作流

```text
输入链接 / 上传原视频 / 补充材料
→ Storyboard Agent
→ Content Creator Agent
→ Review Agent
→ LLM 生成脚本 / 分镜 / prompt
→ Seedance 生成视频片段
→ FFmpeg 合成 final.mp4
→ 字幕烧录 final_subtitled.mp4
→ 页面预览和下载
```

## 当前支持的输入

- 短视频链接，用于平台识别和任务记录。
- 用户自己有权限的本地视频。
- 原视频字幕。
- 原视频文案。
- 画面说明和截图描述。
- 复刻要求和改编边界。
- 目标平台、目标时长、风格和复刻强度。

链接解析只是输入辅助，不等于自动下载视频。解析失败时，用户仍可以通过上传视频和补充材料继续完整流程。

## 当前输出结果

每个任务会生成：

- `final.mp4`：原始成片。
- `final_subtitled.mp4`：带字幕成片。
- `cover.jpg`：封面帧。
- `production-package.md`：制作包。
- `project-package.json`：完整项目包。
- `outputs_manifest.json`：成片资产清单。

网页端支持直接预览 MP4，并提供下载入口。

## 网页端能力

- 创建任务。
- 输入链接、上传原视频、补充原字幕 / 文案 / 画面说明。
- 读取原视频元信息。
- 抽取关键帧。
- Dry-run 预估。
- 一键生成。
- 后台 job 和页面轮询进度。
- 总控审稿和 prompt 修正。
- 旁白稿、字幕稿、SRT 和 mock 音频。
- 字幕烧录。
- 成片预览、封面导出、制作包和 JSON 包下载。
- 历史 jobs 和历史错误日志折叠展示。

## 安全与合规边界

- 不自动下载公开视频。
- 不绕过登录、验证码、反爬、付费墙或平台限制。
- 不默认搬运或复制原视频。
- API Key 只放在客户本地 `.env`。
- 真实生成必须通过成本保护。
- Mock 和真实结果在 JSON 中明确标记。

## 当前限制

- 本地运行，不是公网 SaaS。
- 不包含 API Key。
- 暂无多用户、登录、权限和计费。
- 当前后台 job 是本地 JSON 轻量实现，不是生产级队列。
- TTS 真实调用已预留，但不是稳定主链路。
- ASR 尚未接入真实链路。
- Kling / Luma 是实验性 provider，不作为稳定交付链路。
- 本地数据依赖本机文件系统。

## 交付建议

v2.0 适合用于：

- 客户本地演示。
- 内部内容团队试用。
- 短视频创作流程验证。
- 后续 SaaS 产品化前的功能基线。

不建议直接作为公网多租户生产系统上线。上线前应补齐登录、权限、计费、云存储、生产级队列、审计和运维能力。
