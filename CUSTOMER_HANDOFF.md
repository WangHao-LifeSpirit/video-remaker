# Customer Handoff v1.0

## 产品简介

“短视频复刻自动化工作台”是一个面向短视频内容团队的本地网页端工具。它的目标不是搬运已有视频，而是学习公开视频或用户自有视频材料的结构、节奏和表达方式，再生成“同款结构、原创内容”的短视频制作方案和成片。

当前版本为 v1.0，本地可运行，不部署公网，不包含 API Key。客户需要自行配置 `.env`，并自行管理 DeepSeek、Seedance 等第三方 API 的额度和费用。

## 复刻的 Bloome Agent 群组逻辑

本产品复刻了 Bloome 中 Agent 群组的核心协作方式：

- Parser Agent：整理用户输入、链接、文案、字幕和截图描述。
- Storyboard Agent：拆解视频结构、分镜、节奏和画面语言。
- Content Creator Agent：生成原创改编方向、新脚本、新分镜和视频模型 prompt。
- Quality Check Agent：检查原创性、可执行性、素材缺口和平台适配风险。
- Orchestrator：串联流程、控制步骤、复用已有产物、执行导出和合成。

v1.0 已将这些能力从原型流程落到本地产品中，并提供 CLI 与网页按钮两种使用方式。

## 当前工作流

当前稳定主链路为：

```text
用户输入材料
→ DeepSeek 分析
→ Storyboard 分镜拆解
→ Content Creator 原创改编
→ Seedance 视频片段生成
→ FFmpeg 合成
→ final.mp4
```

同时导出：

```text
production-package.md
project-package.json
```

## 当前支持的输入方式

- 短视频链接或分享文本。
- 用户上传的自有视频文件路径。
- 用户手动补充文案。
- 用户手动补充字幕。
- 用户手动补充截图描述。
- 目标平台、目标时长、风格、复刻强度等创作参数。

当前版本不会自动下载公开视频，也不会绕过平台限制。

## 当前输出结果

每个任务会生成一组结构化产物：

- `task.json`：任务状态和全局信息。
- `input.json`：用户输入材料整理结果。
- `analysis.json`：内容主题、结构、节奏和爆点分析。
- `storyboard.json`：原视频结构分镜拆解。
- `remake_plan.json`：原创改编方向、新脚本、新分镜、发布文案。
- `video_prompts.json`：面向 Seedance 等视频模型的 prompt。
- `assets.json`：视频素材、生成状态、时间线和合成状态。
- `final.mp4`：FFmpeg 合成后的本地视频成片。
- `production-package.md`：可读制作包。
- `project-package.json`：完整项目包。

网页任务详情页可以直接预览 `final.mp4`，并下载最终文件。

## 网页端能力

v1.0 的网页端支持：

- 创建任务。
- 任务详情查看。
- dry-run 成本预估。
- 一键生成。
- 后台 job 执行。
- 页面轮询进度。
- 历史 jobs 展示。
- 防重复运行。
- failed job 重试。
- `final.mp4` 页面内预览。
- `final.mp4`、`production-package.md`、`project-package.json` 下载。
- 历史错误日志折叠展示。
- 历史错误日志安全清理与备份。

## 安全与合规边界

当前产品遵守以下边界：

- 不绕过登录。
- 不绕过验证码。
- 不绕过反爬。
- 不自动下载公开视频。
- 不默认搬运或复制原视频。
- 对公开视频只做结构学习和原创改编。
- API Key 只放在 `.env`。
- 真实视频生成必须通过成本保护开关。
- Mock 和真实结果都在 JSON 中明确标记。

## 成本保护

真实视频生成属于付费能力。v1.0 内置成本保护：

- `ENABLE_PAID_API_CALLS=false` 时不会触发真实视频生成。
- `scene-limit` 不能超过 `MAX_VIDEO_SCENES_PER_RUN`。
- 已成功生成的 Seedance scene 会自动复用。
- 后台已有 queued/running job 时不会重复创建新 job。
- 默认不重复生成已成功 scenes。
- 只有显式开启 `force` 才会重新生成。

## 当前限制

- 暂无多用户、登录、权限和计费。
- 暂无 TTS 配音。
- 暂无 ASR 转写。
- 暂无字幕烧录。
- 暂无上传视频真实处理：ffprobe 元信息、抽帧、字幕读取。
- 暂无完整抖音/短视频链接自动解析。
- 暂无真正生产级任务队列、任务取消、并发控制和服务重启恢复。
- 本地任务依赖本机文件系统。
- Kling / Luma 不是当前稳定交付链路。
- 当前建议限制真实生成到少量 scenes，以控制成本。

## 下一阶段可扩展功能

建议下一阶段优先扩展：

- Agent review cycle：总控审稿、打回重写和质量评分。
- 上传视频真实处理：ffprobe、抽帧、字幕读取。
- TTS 配音生成。
- 字幕烧录。
- 更完整的公开视频链接解析和安全元信息提取。
- 全量多 scene 真实生成。
- 生产级任务队列和实时推送。
- 多用户、登录和计费系统。

## 交付建议

v1.0 适合用于：

- 客户本地演示。
- 内部创作流程验证。
- 小规模视频制作自动化试用。
- 后续 SaaS 产品化前的功能基线。

不建议直接作为公开多租户生产系统上线；上线前需要补齐权限、计费、审计、云存储、任务队列、异常恢复和安全运维能力。
