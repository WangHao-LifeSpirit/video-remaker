# Customer Handoff

## 产品简介

“短视频复刻自动化工作台”是一个面向短视频内容团队的本地自动化工具。它的目标不是搬运已有视频，而是学习公开视频或用户自有视频材料的结构、节奏和表达方式，再生成“同款结构、原创内容”的短视频制作方案和成片。

当前版本是 v0.7，本地可运行，支持从用户输入材料到 AI 分析、原创改编、视频片段生成、FFmpeg 合成和制作包导出的完整闭环。

## 复刻的 Bloome Agent 群组逻辑

本产品复刻了 Bloome 中 Agent 群组的核心协作方式：

- Parser Agent：整理用户输入、链接、文案、字幕和截图描述。
- Storyboard Agent：拆解视频结构、分镜、节奏和画面语言。
- Content Creator Agent：生成原创改编方向、新脚本、新分镜和视频模型 prompt。
- Quality Check Agent：检查原创性、可执行性、素材缺口和平台适配风险。
- Orchestrator：串联流程、控制步骤、复用已有产物、执行导出和合成。

v0.7 已将这些能力从原型流程落到本地产品中，并提供 CLI 与网页按钮两种使用方式。

## 当前工作流

当前主链路为：

```text
用户输入材料
→ DeepSeek 分析
→ Storyboard 分镜拆解
→ Content Creator 原创改编
→ Seedance 视频片段生成
→ FFmpeg 合成
→ final.mp4
```

同时导出：

```text
production-package.md
project-package.json
```

## 当前支持的输入方式

- 短视频链接或分享文本。
- 用户上传的自有视频文件。
- 用户手动补充文案。
- 用户手动补充字幕。
- 用户手动补充截图描述。
- 目标平台、目标时长、风格、复刻强度等创作参数。

当前版本不会自动下载公开视频，也不会绕过平台限制。

## 当前输出结果

每个任务会生成一组结构化产物：

- `task.json`：任务状态和全局信息。
- `input.json`：用户输入材料整理结果。
- `analysis.json`：内容主题、结构、节奏和爆点分析。
- `storyboard.json`：原视频结构分镜拆解。
- `remake_plan.json`：原创改编方向、新脚本、新分镜、发布文案。
- `video_prompts.json`：面向 Seedance 等视频模型的 prompt。
- `assets.json`：视频素材、生成状态、时间线和合成状态。
- `final.mp4`：FFmpeg 合成后的本地视频成片。
- `production-package.md`：可读制作包。
- `project-package.json`：完整项目包。

网页任务详情页可以直接下载最终文件。

## 安全与合规边界

当前产品遵守以下边界：

- 不绕过登录。
- 不绕过验证码。
- 不绕过反爬。
- 不自动下载公开视频。
- 不默认搬运或复制原视频。
- API Key 只放在 `.env`。
- 真实视频生成必须通过成本保护开关。
- Mock 和真实结果都在 JSON 中明确标记。

## 成本保护

真实视频生成属于付费能力。v0.7 内置成本保护：

- `ENABLE_PAID_API_CALLS=false` 时不会触发真实视频生成。
- `scene-limit` 不能超过 `MAX_VIDEO_SCENES_PER_RUN`。
- 已成功生成的 Seedance scene 会自动复用。
- 默认不重复生成 s1/s2/s3。
- 只有显式开启 `force` 才会重新生成。

## 当前限制

- 页面内视频播放器预览尚未完成。
- 后台任务队列和实时进度尚未完成。
- 上传视频抽帧、字幕读取、元信息提取尚未完成。
- TTS 配音尚未完成。
- ASR 转写尚未完成。
- 字幕烧录尚未完成。
- 短视频链接深度解析仍需增强。
- 当前不是多用户 SaaS，没有登录、权限、团队协作和计费模块。
- 当前建议限制真实生成到少量 scenes，以控制成本。

## 下一阶段可扩展功能

建议下一阶段优先扩展：

- 页面内 `final.mp4` 预览播放器。
- 后台任务队列和实时进度。
- 总控 Agent 审稿、打回重写和质量评分。
- 上传视频真实处理：ffprobe、抽帧、字幕读取。
- TTS 配音生成。
- 字幕烧录。
- 更完整的公开视频链接解析和安全元信息提取。
- 全量多 scene 真实生成。
- 多用户、登录和计费系统。

## 交付建议

v0.7 适合用于：

- 客户本地演示。
- 内部创作流程验证。
- 小规模视频制作自动化试用。
- 后续 SaaS 产品化前的功能基线。

不建议直接作为公开多租户生产系统上线；上线前需要补齐任务队列、权限、计费、审计、素材存储和异常恢复能力。
