# Next Steps After v1.0

本文档记录 v1.0 之后的产品开发路线。v1.0 已完成本地网页端工作台、DeepSeek 分析、Seedance 片段生成、FFmpeg 成片、后台 job、页面轮询、视频预览、下载和历史错误日志清理。

## 已在 v1.0 完成

- 页面内 `final.mp4` 预览播放器。
- 后台 job 执行。
- 页面轮询进度。
- 历史 jobs。
- 防重复运行。
- failed job 重试。
- 历史错误日志折叠与清理。

## 1. Agent Review Cycle，总控审稿与打回重写

目标：让 Orchestrator 不只是串流程，还能做质量判断和返工。

建议实现：

- Quality Check Agent 输出评分。
- 检查是否过于相似。
- 检查是否符合目标平台。
- 检查素材是否缺失。
- 检查脚本是否可执行。
- 低分时自动要求 Content Creator 重写。
- 保留 review history。

价值：提升成片质量和原创安全性。

## 2. 上传视频处理，抽帧、字幕、元信息

目标：用户上传自有视频后，系统能真实理解视频素材。

建议实现：

- ffprobe 读取：
  - 时长
  - 分辨率
  - 帧率
  - 音轨信息
- FFmpeg 抽取关键帧。
- 读取字幕文件。
- 将 frames 和 metadata 写入 `input.json`。
- 后续给 LLM 提供更准确的分析上下文。

价值：从“用户文字补充”升级到“真实视频材料分析”。

## 3. TTS 配音

目标：根据新脚本自动生成旁白音频。

建议实现：

- TTS provider 配置。
- 没 Key 时生成静音或 mock 音轨。
- 有 Key 时生成 `voiceover.mp3` 或 `voiceover.wav`。
- 写入 `assets.json`。
- FFmpeg 合成时使用真实旁白。

价值：让 final.mp4 更接近可发布成片。

## 4. 字幕烧录

目标：将字幕真正烧录进视频。

建议实现：

- 根据 remake plan 或字幕稿生成 SRT。
- 支持样式配置：
  - 字体大小
  - 位置
  - 描边
  - 高亮关键词
- FFmpeg burn-in。
- 保留外挂字幕文件。

价值：短视频发布场景通常需要内嵌字幕。

## 5. 抖音/短视频链接解析增强

目标：更稳定地处理公开视频链接，但继续遵守安全边界。

建议实现：

- 解析官方短链到最终 URL。
- 提取平台、content_id、公开标题等安全元信息。
- 不绕过登录、验证码、反爬。
- 不自动下载视频。
- 失败时进入用户补充材料模式。

价值：降低用户手动补充成本。

## 6. 多 scene 全量真实生成

目标：从受限前 3 个 scenes 扩展到完整分镜真实生成。

建议实现：

- 更严格的成本预估。
- 分批生成。
- 单 scene 失败 fallback mock。
- 允许用户选择要重生成的 scene。
- 支持只重生成某个片段。
- 展示每个 scene 的远程任务状态。

价值：提升 final.mp4 的完整真实素材比例。

## 7. 生产级任务队列

目标：把当前本地 JSON job 机制升级为更可靠的生产基础设施。

建议实现：

- 持久化队列。
- 并发限制。
- 任务取消。
- 任务超时处理。
- 服务重启恢复。
- Job 日志和审计。
- 实时推送，替代纯轮询。

价值：为团队使用和长任务稳定执行打基础。

## 8. 多用户、登录、计费

目标：从本地工作台升级为 SaaS 产品。

建议实现：

- 用户登录。
- 项目/任务隔离。
- API Key 管理。
- 额度和账单。
- 生成成本记录。
- 权限控制。
- 云存储。
- 审计日志。

价值：进入商业化和团队协作阶段。

## 推荐节奏

短期：

1. Agent review cycle。
2. 上传视频处理。
3. TTS 配音。

中期：

1. 字幕烧录。
2. 链接解析增强。
3. 多 scene 全量真实生成。

长期：

1. 生产级任务队列。
2. 多用户、登录、计费。
3. 云部署和团队协作。
