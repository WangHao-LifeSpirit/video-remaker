# Next Steps

本文档记录 v0.7 之后的产品开发路线。排序按交付价值、当前缺口和工程依赖关系排列。

## 1. 页面内视频预览播放器

目标：任务详情页直接预览 `final.mp4`。

建议实现：

- 在任务详情页增加视频播放器。
- 支持重新加载最新成片。
- 显示 MP4 路径、时长、分辨率和生成时间。
- 下载按钮保留。

价值：让客户不离开网页就能检查结果，是最直接的体验提升。

## 2. 后台任务队列和实时进度

目标：一键生成不阻塞网页请求，页面能实时看到每一步进度。

建议实现：

- 本地任务队列。
- 任务运行状态表或 JSON 状态流。
- Server-Sent Events 或轮询。
- 步骤级状态：
  - analyze
  - storyboard
  - remake
  - prompts
  - generate-assets
  - assemble
  - export
- 失败可恢复。

价值：真实视频生成耗时较长，后台队列是产品化的关键基础设施。

## 3. Agent Review Cycle，总控审稿与打回重写

目标：让 Orchestrator 不只是串流程，还能做质量判断和返工。

建议实现：

- Quality Check Agent 输出评分。
- 检查是否过于相似。
- 检查是否符合目标平台。
- 检查素材是否缺失。
- 检查脚本是否可执行。
- 低分时自动要求 Content Creator 重写。
- 保留 review history。

价值：提升成片质量和原创安全性。

## 4. 上传视频处理，抽帧、字幕、元信息

目标：用户上传自有视频后，系统能真实理解视频素材。

建议实现：

- ffprobe 读取：
  - 时长
  - 分辨率
  - 帧率
  - 音轨信息
- FFmpeg 抽取关键帧。
- 读取字幕文件。
- 将 frames 和 metadata 写入 `input.json`。
- 后续给 LLM 提供更准确的分析上下文。

价值：从“用户文字补充”升级到“真实视频材料分析”。

## 5. TTS 配音

目标：根据新脚本自动生成旁白音频。

建议实现：

- TTS provider 配置。
- 没 Key 时生成静音或 mock 音轨。
- 有 Key 时生成 `voiceover.mp3` 或 `voiceover.wav`。
- 写入 `assets.json`。
- FFmpeg 合成时使用真实旁白。

价值：让 final.mp4 更接近可发布成片。

## 6. 字幕烧录

目标：将字幕真正烧录进视频。

建议实现：

- 根据 remake plan 或字幕稿生成 SRT。
- 支持样式配置：
  - 字体大小
  - 位置
  - 描边
  - 高亮关键词
- FFmpeg burn-in。
- 保留外挂字幕文件。

价值：短视频发布场景通常需要内嵌字幕。

## 7. 抖音/短视频链接解析增强

目标：更稳定地处理公开视频链接，但继续遵守安全边界。

建议实现：

- 解析官方短链到最终 URL。
- 提取平台、content_id、公开标题等安全元信息。
- 不绕过登录、验证码、反爬。
- 不自动下载视频。
- 失败时进入用户补充材料模式。

价值：降低用户手动补充成本。

## 8. 多 scene 全量真实生成

目标：从前 3 个 scenes 扩展到完整分镜真实生成。

建议实现：

- 更严格的成本预估。
- 分批生成。
- 单 scene 失败 fallback mock。
- 允许用户选择要重生成的 scene。
- 支持只重生成某个片段。
- 展示每个 scene 的远程任务状态。

价值：提升 final.mp4 的完整真实素材比例。

## 9. 多用户、登录、计费

目标：从本地工作台升级为 SaaS 产品。

建议实现：

- 用户登录。
- 项目/任务隔离。
- API Key 管理。
- 额度和账单。
- 生成成本记录。
- 权限控制。
- 云存储。
- 审计日志。

价值：进入商业化和团队协作阶段。

## 推荐节奏

短期：

1. 页面内视频预览播放器。
2. 后台任务队列和实时进度。
3. Agent review cycle。

中期：

1. 上传视频处理。
2. TTS 配音。
3. 字幕烧录。

长期：

1. 链接解析增强。
2. 多 scene 全量真实生成。
3. 多用户、登录、计费。
