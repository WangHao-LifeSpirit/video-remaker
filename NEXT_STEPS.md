# Next Steps After v2.1

本文档记录 v2.1 之后的产品开发路线。v2.1 已完成本地网页端工作台、LLM Provider 分析、Seedance 片段生成、FFmpeg 成片、字幕烧录、后台 job、页面轮询、视频预览、安全的模型设置和 macOS 一键启动。

## 已完成到 v2.1

- 页面内 `final.mp4` 预览播放器。
- 后台 job 执行。
- 页面轮询进度。
- 历史 jobs。
- 防重复运行。
- failed job 重试。
- 历史错误日志折叠与清理。
- Agent review cycle 基础版：生成 `review_report.json`，可选应用 prompt 建议。
- 总控审稿按钮化。
- 旁白稿、字幕稿、SRT 和 mock/silent 音频。
- 字幕烧录，输出 `final_subtitled.mp4`。
- 上传本地原视频。
- ffprobe 读取原视频元信息。
- FFmpeg 抽取原视频关键帧。
- 保存原视频字幕、文案、画面说明和复刻要求。
- 导出封面帧 `cover.jpg`。
- 生成 `outputs_manifest.json` 并在页面展示成片资产。
- 安全链接解析辅助，输出 `source_link.json` 并引导上传/补充材料。

## 1. Agent Review Cycle 增强，总控打回重写

目标：在 v1.1 基础审稿之上，让 Orchestrator 能自动返工和保留审稿历史。

建议实现：

- 多轮 review history。
- 低分时自动要求 Content Creator 重写 scene。
- 对 storyboard / remake_plan / video_prompts 分别给出打回目标。
- 页面按钮触发审稿和 apply。
- 审稿前后 prompt diff。
- 保留 review history。

价值：提升成片质量和原创安全性。

## 2. 真实 ASR 转写

目标：从用户上传视频中自动生成原视频字幕，减少手动补充成本。

建议实现：

- ASR provider 配置和成本保护。
- 读取上传视频音轨。
- 输出原视频 transcript。
- 将 transcript 写入 `input.json`。
- 失败时继续允许用户手动补充字幕。

价值：让上传视频分析更完整，但仍不下载或搬运公开视频。

## 3. 真实 TTS 配音

目标：根据新脚本自动生成旁白音频。

建议实现：

- TTS provider 配置。
- 没 Key 时生成静音或 mock 音轨。
- 有 Key 时生成 `voiceover.mp3` 或 `voiceover.wav`。
- 写入 `assets.json`。
- FFmpeg 合成时使用真实旁白。

价值：让 final.mp4 更接近可发布成片。

## 4. 字幕烧录增强

目标：在已有基础字幕烧录之上增加更适合短视频的字幕样式。

建议实现：

- 支持样式配置：
  - 字体大小
  - 位置
  - 描边
  - 高亮关键词
- 多行排版和关键词高亮。
- 多语言字幕样式模板。

价值：提升成片可读性和发布观感。

## 5. 短视频链接解析增强

目标：在 v1.5 本地平台识别基础上，更稳定地处理公开链接元信息，但继续遵守安全边界。

建议实现：

- 可选解析官方短链到最终 URL。
- 可选提取公开 HTML title 等低风险元信息。
- 为不同平台补充 content_id 识别规则。
- 不绕过登录、验证码、反爬。
- 不自动下载视频。
- 失败时进入用户补充材料模式。

价值：降低用户手动补充成本。

## 6. 多 scene 全量真实生成

目标：从受限前 3 个 scenes 扩展到完整分镜真实生成。

建议实现：

- 更严格的成本预估。
- 分批生成。
- 单 scene 失败 fallback mock。
- 允许用户选择要重生成的 scene。
- 支持只重生成某个片段。
- 展示每个 scene 的远程任务状态。

价值：提升 final.mp4 的完整真实素材比例。

## 7. 生产级任务队列

目标：把当前本地 JSON job 机制升级为更可靠的生产基础设施。

建议实现：

- 持久化队列。
- 并发限制。
- 任务取消。
- 任务超时处理。
- 服务重启恢复。
- Job 日志和审计。
- 实时推送，替代纯轮询。

价值：为团队使用和长任务稳定执行打基础。

## 8. 多用户、登录、计费

目标：从本地工作台升级为 SaaS 产品。

建议实现：

- 用户登录。
- 项目/任务隔离。
- API Key 管理。
- 额度和账单。
- 生成成本记录。
- 权限控制。
- 云存储。
- 审计日志。

价值：进入商业化和团队协作阶段。

## 推荐节奏

短期：

1. 真实 ASR 转写。
2. 真实 TTS 配音。
3. Agent review cycle 多轮打回。

中期：

1. 字幕烧录增强。
2. 链接解析增强。
3. 多 scene 全量真实生成。

长期：

1. 生产级任务队列。
2. 多用户、登录、计费。
3. 云部署和团队协作。
